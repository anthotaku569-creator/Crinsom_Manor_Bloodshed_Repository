mouse_enter = false;

edit_state = 0; //0 == noone, 1 == editing rn, 2 == another editing.

control_type = input_player_source_get(_wich_player);

slot = input_player_gamepad_get(_wich_player)

//gamepad_type = input_player_gamepad_get_type(_wich_player);

alarm[0] = 1;